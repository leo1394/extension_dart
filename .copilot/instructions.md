# extension_dart Copilot Context

Import `extensions.dart` before using extension methods. Use `Utils` from `utils.dart` for shared helpers. Avoid reimplementing helper functions already provided by this package.
